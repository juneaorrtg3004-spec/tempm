name = "temp-mail-worker"
main = "src/index.ts"
compatibility_date = "2024-12-01"
workers_dev = true
account_id = "76294535923ea3237ca938a93ecc6c50"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "temp-mail-db"
database_id = "d3ccf28c-1b5f-4b46-82cb-964036d87795"

[triggers]
crons = ["0 3 * * *"]

[vars]
# Comma-separated list of allowed frontend origins (for CORS)
ALLOWED_ORIGINS = "http://localhost:3000"
# ADMIN_PASSWORD is stored as a Wrangler secret (not in this file).
# Set it with: cd worker && npx wrangler secret put ADMIN_PASSWORD
ADMIN_PASSWORD = "ph19911991"